[project]
name = "fpl-analytics"
version = "0.1.0"
description = "ETL pipeline and predictive models for Fantasy Premier League"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
    "pandas>=2.2",
]

[tool.setuptools.packages.find]
where = ["src"]